#!/usr/bin/env node

'use strict';

var path = require('path');
var einrcored = require('einrcore-node/lib/cli/bitcored');
var servicesPath = path.resolve(__dirname, '../');
var additionalServices = ['insight-einr-api', 'insight-einr-ui'];
einrcored(servicesPath, additionalServices);
